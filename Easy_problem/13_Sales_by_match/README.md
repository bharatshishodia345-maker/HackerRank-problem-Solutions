# Sock Merchant/Salse by match

## 📌 Problem

Given an array representing sock colors, find the total number of matching pairs.

Two socks form a pair when they have the same color.

### Example

Input:

```text
n = 9
ar = [10, 20, 20, 10, 10, 30, 50, 10, 20]